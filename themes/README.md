# themes/

Reserved for alternate theme presets (token overrides) layered on top of the
base tokens in `tokens/index.css` — e.g. a higher-contrast clinical theme or a
per-project accent swap. Empty for now — populated in a later pass.
